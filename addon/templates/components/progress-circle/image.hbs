<clipPath id="clip-circle-{{uniqeID}}">
  <circle
    cx="50"
    cy="50"
    r="{{radius}}"
  />
</clipPath>

<image
  xlink:href="{{imageUrl}}"
  clip-path="url(#clip-circle-{{uniqeID}})"
  height={{position}} width={{position}}
  x={{strokeWidth}}
  y={{strokeWidth}}
>
</image>
